/*
 * ReferencesPage Messages
 *
 * This contains all the text for the ReferencesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReferencesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReferencesPage container!',
  },
});
