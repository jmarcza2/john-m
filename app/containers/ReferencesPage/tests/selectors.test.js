// import { selectReferencesPageDomain } from '../selectors';

describe('selectReferencesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
