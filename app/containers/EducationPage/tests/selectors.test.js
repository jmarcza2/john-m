// import { selectEducationPageDomain } from '../selectors';

describe('selectEducationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
