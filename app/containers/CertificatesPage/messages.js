/*
 * CertificatesPage Messages
 *
 * This contains all the text for the CertificatesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CertificatesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CertificatesPage container!',
  },
});
