// import { selectCertificatesPageDomain } from '../selectors';

describe('selectCertificatesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
