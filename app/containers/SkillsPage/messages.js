/*
 * SkillsPage Messages
 *
 * This contains all the text for the SkillsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SkillsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'These skills where exported from LinkedIn, the values are not fully accurate and the values are fake!!!',
  },
});
