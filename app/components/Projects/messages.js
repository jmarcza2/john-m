/*
 * Projects Messages
 *
 * This contains all the text for the Projects component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Projects';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Projects component!',
  },
});
