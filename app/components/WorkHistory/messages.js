/*
 * WorkHistory Messages
 *
 * This contains all the text for the WorkHistory component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WorkHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkHistory component!',
  },
});
