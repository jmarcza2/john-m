/*
 * Certificates Messages
 *
 * This contains all the text for the Certificates component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Certificates';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Certificates component!',
  },
});
