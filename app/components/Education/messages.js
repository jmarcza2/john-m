/*
 * Education Messages
 *
 * This contains all the text for the Education component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Education';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Education component!',
  },
});
